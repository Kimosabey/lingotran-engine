{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "BASE=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\"; echo \"ignore-check base=$BASE (VERCEL_GIT_PREVIOUS_SHA=${VERCEL_GIT_PREVIOUS_SHA:-unset})\"; git rev-parse \"$BASE\" >/dev/null 2>&1 || exit 1; git diff --quiet \"$BASE\" HEAD -- ."
}
